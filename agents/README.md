# Agents

This directory will contain local Python automation agents.

- `health-agent/`: collects system and service health and later sends telemetry to AWS.
- `backup-agent/`: archives allowlisted configuration and later uploads encrypted backups to S3.
