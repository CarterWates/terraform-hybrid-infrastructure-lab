# Backup Agent

Planned Python agent for encrypted configuration backups.

The agent will archive allowlisted non-secret files and upload them to the Terraform-managed S3 bucket after the AWS foundation is implemented.
